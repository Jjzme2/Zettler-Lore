export const ROLES = {
    ADMIN: 'admin',
    MEMBER: 'member',
    FREE: 'free'
} as const

export const ACCESS_LEVELS = {
    PUBLIC: 'public',
    MEMBER: 'member'
} as const

export const STORY_STATUS = {
    DRAFT: 'draft',
    PUBLISHED: 'published',
    ARCHIVED: 'archived'
} as const
