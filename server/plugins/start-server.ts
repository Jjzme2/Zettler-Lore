

export default defineNitroPlugin((nitroApp) => {
    const startServer = () => {
        console.log('Core Zettler Lore server services initialized.')
        // Any verify connections or warm-up logic can go here
    }

    startServer()
})
